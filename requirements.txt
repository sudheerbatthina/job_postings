# jobspy sub-dependencies listed directly so we can relax numpy's broken ==1.26.3 pin.
# Install order matters — see README / install notes:
#   pip install -r requirements.txt
#   pip install python-jobspy --no-deps
# The second command intentionally bypasses jobspy's numpy==1.26.3 metadata pin,
# which has no prebuilt wheel for Python 3.13 on Windows.
numpy>=2.1,<3
pandas>=2.1,<3.0
beautifulsoup4>=4.12.2,<5.0.0
markdownify>=0.13.1,<0.14.0
regex>=2024.4.28,<2025.0.0
pydantic>=2.3.0,<3.0.0
requests>=2.31.0,<3.0.0
tls-client>=1.0.1,<2.0.0
openpyxl>=3.1
pdfplumber>=0.11
